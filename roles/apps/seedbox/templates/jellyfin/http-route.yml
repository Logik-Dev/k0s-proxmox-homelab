---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
  namespace: seedbox
spec:
  hostnames: ['jellyfin.{{ env }}.{{ domain }}']
  parentRefs:
    - name: contour
      namespace: projectcontour
      sectionName: internal-https
  rules:
    - backendRefs:
        - name: jellyfin
          port: 80
